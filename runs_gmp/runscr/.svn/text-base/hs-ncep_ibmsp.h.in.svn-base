#!/bin/sh
#
# @ job_type = serial
# @ output = %EXECSUBDIR%/out.$(jobid)
# @ error = %EXECSUBDIR%/out.$(jobid)
# @ class = dev
# @ preferences = (( Feature == "dev" ) && ( Memory > 256 ))
# @ queue
#
